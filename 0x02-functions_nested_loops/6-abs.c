#include "main.h"
/*Comutes the absolute valuse of an integer*/

/**
 * _abs - function name
 * @a: integer computed
 * Return: int type
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}
