#include "main.h"
/*Comutes the absolute valuse of an integer*/

/**
 * _abs - function name
 * @int: integer computed
 * Return: int type
 */
int _abs(int)
{
	int a;

	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}
