#include <stdio.h>
/*single digits 0-9*/

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	a = 0;
	while (a < 10)
	{
		printf("%d", a"\n");
		a++;
	}
	return (0);
}
